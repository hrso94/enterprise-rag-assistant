# RAG Agent — Web UI

Minimal Next.js front-end for the RAG agent. Provides:
- Upload input for documents (client reads file content and sends to API)
- Ask input for sending a question and displaying the answer

Run locally:

```bash
cd web
npm install
npm run dev
```
